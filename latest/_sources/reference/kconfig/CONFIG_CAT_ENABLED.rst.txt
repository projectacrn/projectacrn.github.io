:orphan:

.. title:: CAT_ENABLED

.. option:: CONFIG_CAT_ENABLED

*Enable CAT (Cache Allocation Technology)*

Type: ``bool``

Help
====

When set in platforms that support CAT, hypervisor can allocate
various amount of last-level-cache (LLC) resources to VMs to achieve
different Class of Service (COS, or CLOS).

Defaults
========

- n

Kconfig definition
==================

.. highlight:: kconfig

At ``arch/x86/Kconfig:247``

Included via ``Kconfig:3``

Menu path: (top menu)

.. parsed-literal::

    config CAT_ENABLED
    	bool "Enable CAT (Cache Allocation Technology)"
    	default n
    	help
    	  When set in platforms that support CAT, hypervisor can allocate
    	  various amount of last-level-cache (LLC) resources to VMs to achieve
    	  different Class of Service (COS, or CLOS).

*(Definitions include propagated dependencies, including from if's and menus.)*